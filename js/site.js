jQuery(document).ready(function($) {

		// Slick example
		/*
		$('.the-slider').slick({
			dots: true,
			infinite: true,
			arrows: true
		});
		*/

});

$(window).resize(function(){
	
	// on resize

});